from ..config.init_material_config import gen_image_path
from .utils import (
  add_material_map, 
  add_material
)

def Mavuika (config, image_path_prefix):
  add_material_map(config, 'Leather', 'Body')
 